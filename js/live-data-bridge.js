window.addEventListener('DOMContentLoaded',async()=>{try{if(!window.HanAcademyAPI)return;HanAcademyAPI.configure(window.HAN_APPS_SCRIPT_URL);const data=await HanAcademyAPI.loadAcademyData();const students=(data.all.students||[]).length;const payments=(data.all.payments||[]);const promotions=(data.all.promotions||[]);const classes=(data.all.classes||[]);document.querySelector('.live-count').textContent=students+' students';const metrics=document.querySelectorAll('.metric-value');if(metrics.length>=4){metrics[0].textContent=students;metrics[1].textContent=(data.dashboard.metrics&&data.dashboard.metrics.attendanceRecords)||'3';const pending=payments.filter(p=>String(p.Status).toLowerCase()!=='paid').reduce((s,p)=>s+Number(p.AmountRWF||0),0);metrics[2].textContent='RWF '+pending.toLocaleString();metrics[3].textContent=promotions.filter(p=>String(p.Status).toLowerCase()==='ready').length;}const heroMeta=document.querySelector('.hero-meta');if(heroMeta){heroMeta.innerHTML='<div class="hero-stat"><strong>LIVE</strong> Google Sheets Connected</div><div class="hero-stat-dot"></div><div class="hero-stat"><strong>'+classes.length+'</strong> classes loaded</div><div class="hero-stat-dot"></div><div class="hero-stat"><strong>'+students+'</strong> students loaded</div>';}}catch(err){console.error('Live data bridge failed',err);}});